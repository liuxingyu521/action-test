{
  "name": "action-test",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "exports": {
    "default": "./dist/index.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "pnpm clean-dist && cp index.js dist/index.js",
    "clean-dist": "rimraf dist && mkdir dist"
  },
  "keywords": [],
  "author": "liuxingyu521",
  "license": "ISC",
  "dependencies": {
    "cowsay": "^1.6.0",
    "rimraf": "^6.0.1"
  }
}
